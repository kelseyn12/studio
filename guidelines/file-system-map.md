# File system map

- `app/` routes: Today, pipeline, plan, calendar, campaigns, cards, CapCut in, library, repurposer, transcriber, dms, analytics, connections, team
- `app/api/` auth, files, assets, Outstand connect/sync, analytics sync, plan move, AI hook, transcribe, repurpose generate
- `components/` shell, nav, pills, action card, studio map, today board, plan board, hook rewrite, refresh stats, batch settings, clip tile, editor need, calendar board, day slot, post chip, card brief/footage/editor/live
- `lib/` prisma, pipeline, deals, next-action, session, Outstand, analytics, formats, rewrite, whisper, manychat, ffmpeg, variations, combinations, editor-packet, queries, publish
- `prisma/schema.prisma` studio data model
- `data/` SQLite database and uploads
- `guidelines/` conventions, map, glossary, todo
